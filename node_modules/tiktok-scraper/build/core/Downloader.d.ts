/// <reference types="node" />
import { MultipleBar } from '../helpers';
import { DownloaderConstructor, PostCollector, ZipValues } from '../types';
export declare class Downloader {
    progress: boolean;
    mbars: MultipleBar;
    progressBar: any[];
    private proxy;
    test: boolean;
    noWaterMark: boolean;
    userAgent: string;
    filepath: string;
    bulk: boolean;
    constructor({ progress, proxy, test, noWaterMark, userAgent, filepath, bulk }: DownloaderConstructor);
    private get getProxy();
    addBar(len: number): any[];
    toBuffer(item: PostCollector): Promise<Buffer>;
    zipIt({ collector, filepath, fileName, asyncDownload }: ZipValues): Promise<unknown>;
    downloadSingleVideo(post: PostCollector): Promise<void>;
}
